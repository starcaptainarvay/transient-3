extern float tick;
extern float delta;

vec4 effect(vec4 color, Image texture, vec2 texture_coords, vec2 screen_coords) {

    //vec4 original_pixel = Texel(texture, texture_coords);
   


    return vec4(1.0, 1.0, 1.0, 1.0); // original_pixel * color ; // vec4(0.5, 0.5, 1.0, 1.0);
}